import { LoginForm } from "@/components/admin/LoginForm";

export default function AdminLoginPage() {
  return (
    <main className="fixed inset-0 flex items-center justify-center bg-neutral-950">
      <LoginForm />
    </main>
  );
}
